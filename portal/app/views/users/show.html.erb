<p id="notice"><%= notice %></p>

<%# current_user.pull_facebook_friends %>

<%# @friendlist = [] %>

<%# current_user.friends.each do |friend|  %>
  <%# if !User.where(name: friend.values[0]).empty? %>
   <%# @friendlist << friend.values[0] %>
  <%# end  %>
<%# end  %>

<br><%#= "Your Friends Using LunchtYme are #{@friendlist}" %>


<br><br>Personal Information <br>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Class year:</strong>
  <%= @user.class_year %>
</p>

<p>
  <strong>Res college:</strong>
  <%= @user.res_college %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @user.phone %>
</p>

<p>
  <strong>Gender:</strong>
  <%= @user.gender %>
</p>



<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<% if @user != @current_user %>
  <% if @user.matchings.find_by(match_id: @current_user.id, status: 'accepted') %>
      Already Matched
  <% elsif @user.matchings.find_by(match_id: @current_user.id, status: 'pending') %>
      Lunch Request Pending
  <% elsif @user.matchings.find_by(match_id: @current_user.id, status: 'requested') %>
      <%= link_to 'Request Lunch', matchings_path(match_id: @current_user.id), method: :post %>
  <% else %>
      <%= link_to 'Request Lunch', matchings_path(match_id: @current_user.id), method: :post %>
  <% end %>
<% end %>
